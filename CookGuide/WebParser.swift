//
//  WebParser.swift
//  CookGuide
//
//  Created by Kenneth Skelton on 11/17/21.
//

import Foundation
import WebKit
import SwiftUI


//Depreciated
func findAllIngredients(){
    /*
    let TestURL = "https://www.allrecipes.com/recipe/13477/double-layer-pumpkin-cheesecake/"
    
    print(TestURL)
    let webView: WKWebView = {
        let prefs = WKWebpagePreferences()
        let config = WKWebViewConfiguration()
      
        prefs.allowsContentJavaScript = true
        
        config.defaultWebpagePreferences = prefs
        let webView = WKWebView(frame: .zero, configuration: config)
        return webView
        
    }()
    
    print(webView)
    
    guard let urlString = URL(string: TestURL) else{
        print("URL creation failed")
        return
    }
    
    print(urlString)
    
    
    webView.load(URLRequest(url: urlString))
    
    webView.evaluateJavaScript("document.body"){result, error in guard let html = result, error == nil else {
        print("Failed in evaluate javascript")
        return
        }
        print(html)
    }
    
    
    */
    
}




